import pandas as pd
import logging

logging.basicConfig(
    level=logging.INFO,  # Make sure INFO level messages are shown
    format="%(asctime)s - %(levelname)s - %(message)s"
)

# Finalized schema
# name, age, gender, date_missing (YYYY-MM-DD), last_seen, lat, long

if __name__ == "__main__":
    pass